<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Homework</title>
</head>

<body>
    <!--
Обязательное задание.

Необходимо вывести в консоль значения выражений:
10 + 10 + "10"
10 + "10" + 10
10 + 10 + +"10"
10 / -""
10 / +"2,5"
В этих выражениях нет опечаток, вам их нужно скопировать и вывести в консоль.

Над каждым выводом в консоль необходимо объяснить почему мы получаем такой
результат.

Объяснения пишите в таком формате:
/*
1. 4 умножаем на 2 получаем 8.
2. 3 минус 8 получаем -5.
3. С помощью console.log выводим число -5 в консоль.
*/
console.log(3 - 4 * 2);
-->
    <script>
        "use strict";

        /*
        1. Первым действием сложаться два числа 10 + 10, получим 20
        2. Вторым действием увидим текст и при помощи операции конкатенации получим "2010"
        */
        console.log(10 + 10 + "10")
        /*
        1. В первом же действии второе слагаемое текст, значит все преобразуется в текстовый тип данных, получим "1010"
        2. Опять string + string: "1010" + "10" = "101010"
        */
        console.log(10 + "10" + 10)
        /*
        1. В первом дествии видим два числа, складываем, получаем 20
        2. Дальше число + строка с унарным сложением, унарое сложение преобразует 
           строку "10" в число, и скалдывает 20 + 10 = 30
        */
        console.log(10 + 10 + + "10")
        /*
        1. Унарный минус перед операндом "" преобразует его в 0
        2. Результатом деления 10 / 0 = infinity
        */
        console.log(10 / -"")
        /*
        1. Первым делом пытаемся преоразовать "2,5" в число, не получается, т.к. стоит запятая, а не точка = NaN
        2. Арифметические операции с NaN дают NaN: 10 / NaN = NaN
        */
        console.log(10 / -"2,5")
    </script>
</body>

</html>
